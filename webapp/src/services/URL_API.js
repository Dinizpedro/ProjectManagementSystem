// protocol
const protocol = window.location.protocol + "//"

// localhost
const host = window.location.hostname

// source: https://developer.mozilla.org/en-US/docs/Web/API/Location#wikiArticle

export const URL_API = protocol + host + ':8080'
